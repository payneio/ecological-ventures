<%= form_with(model: ecosystem) do |form| %>
  <% if ecosystem.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ecosystem.errors.count, "error") %> prohibited this ecosystem from being saved:</h2>

      <ul>
        <% ecosystem.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: "form-label" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= form.label :description, class: "form-label" %>
    <%= form.text_area :description, rows: 10, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= form.label :link, class: "form-label" %>
    <%= form.text_field :link, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= form.label :cover_photo, class: "form-label" %>
    <%= form.text_field :cover_photo, class: "form-control" %>
  </div>

  <div class="btn-group">
    <%= form.submit class: "btn btn-primary" %>
    <%= link_to "Cancel", :back, class: "btn btn-secondary" %>
  </div>
<% end %>
