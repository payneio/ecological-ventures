<div class="position-fixed bottom-0 end-0 p-3" style="z-index: 11">
    <div class="toast-container">
        <% flash.map do |type, message| %>
            <%= render partial: "shared/toast", locals: { type: type, message: message } if message %>
        <% end %>
    </div>
</div>